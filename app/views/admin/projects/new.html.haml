.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_projects_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_project_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Project
    .inner
      = form_for :project, :url => admin_projects_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
